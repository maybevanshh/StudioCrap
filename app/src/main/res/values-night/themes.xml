<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AndroidClass" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/Primary_body</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/Primary_body</item>
    </style>
</resources>